# RWB E-Library - Google Apps Script Integration Guide

## 📚 Overview
เว็บไซต์ RWB E-Library มีการเชื่อมต่อ Google Apps Script ใน 4 จุด:

### 1. **ปุ่มค้นหาหนังสือในหน้าแรก** - สีส้มไล่เฉด (Orange Gradient)
### 2. **ปุ่มรายชื่อนักเรียนค้างคืนหนังสือในหน้าแรก** - สีเทาดำไล่เฉด (Dark Gray Gradient)
### 3. **ปุ่มค้นหาใน Navbar** - เมื่อกดปุ่ม "ค้นหา" หรือกด Enter
### 4. **เมนู "รายชื่อนักเรียนค้างคืนหนังสือ" ใน Navbar**

---

## 🔗 การติดตั้ง URL ของ Google Apps Script

### ขั้นตอนที่ 1: สร้าง Google Apps Script Web App
1. เปิด Google Sheets ที่เก็บข้อมูลหนังสือและข้อมูลการยืม
2. ไปที่ Extensions > Apps Script
3. สร้าง Web App โดยใช้ `doGet()` function
4. Deploy as Web App และเลือก "Anyone" สำหรับ access
5. คัดลอก URL ที่ได้จาก Deploy

### ขั้นตอนที่ 2: วาง URL ลงในโค้ด

#### 📍 ไฟล์ที่ 1: `/components/HomePage.tsx`

**สำหรับปุ่มค้นหาหนังสือ (บรรทัดที่ ~279):**
```tsx
<a 
  href="YOUR_GOOGLE_APPS_SCRIPT_SEARCH_URL_HERE" 
  target="_blank" 
  rel="noopener noreferrer"
>
```

แก้เป็น:
```tsx
<a 
  href="https://script.google.com/macros/s/YOUR_SCRIPT_ID_HERE/exec?page=search" 
  target="_blank" 
  rel="noopener noreferrer"
>
```

**สำหรับปุ่มรายชื่อนักเรียนค้างคืนหนังสือ (บรรทัดที่ ~311):**
```tsx
<a 
  href="YOUR_GOOGLE_APPS_SCRIPT_LATE_RETURNS_URL_HERE" 
  target="_blank" 
  rel="noopener noreferrer"
>
```

แก้เป็น:
```tsx
<a 
  href="https://script.google.com/macros/s/YOUR_SCRIPT_ID_HERE/exec?page=lateReturns" 
  target="_blank" 
  rel="noopener noreferrer"
>
```

#### 📍 ไฟล์ที่ 2: `/components/Navbar.tsx`

**สำหรับปุ่มค้นหา (บรรทัดที่ ~23-26):**
```tsx
const handleSearch = (e?: React.FormEvent) => {
  if (e) e.preventDefault();
  // Redirect to Google Apps Script instead of internal page
  const searchUrl = `YOUR_GOOGLE_APPS_SCRIPT_SEARCH_URL_HERE?q=${encodeURIComponent(searchQuery)}&category=${category}`;
  window.open(searchUrl, '_blank');
};
```

แก้เป็น:
```tsx
const handleSearch = (e?: React.FormEvent) => {
  if (e) e.preventDefault();
  const searchUrl = `https://script.google.com/macros/s/YOUR_SCRIPT_ID_HERE/exec?page=search&q=${encodeURIComponent(searchQuery)}&category=${category}`;
  window.open(searchUrl, '_blank');
};
```

**สำหรับเมนู "รายชื่อนักเรียนค้างคืนหนังสือ" (บรรทัดที่ ~247-258):**
```tsx
<a 
  href="YOUR_GOOGLE_APPS_SCRIPT_LATE_RETURNS_URL_HERE"
  target="_blank"
  rel="noopener noreferrer"
  ...
>
  รายชื่อนักเรียนค้างคืนหนังสือ
</a>
```

แก้เป็น:
```tsx
<a 
  href="https://script.google.com/macros/s/YOUR_SCRIPT_ID_HERE/exec?page=lateReturns"
  target="_blank"
  rel="noopener noreferrer"
  ...
>
  รายชื่อนักเรียนค้างคืนหนังสือ
</a>
```

---

## 🎨 ดีไซน์ปุ่มตาม Chulabook.com

### ปุ่มค้นหาหนังสือ (สีส้ม):
- **พื้นหลัง**: Gradient สีส้ม (#FF6B35 → #FF8C42 → #FFB366)
- **ไอคอน**: รูปแว่นขยาย (Search Icon) สีขาว
- **ข้อความ**: "ค้นหาหนังสือ" และคำอธิบาย "ค้นหาหนังสือ ผู้แต่ง ISBN / ระบบ Dewey Decimal"
- **ปุ่มกด**: พื้นขาวโปร่งใส ข้อความสีส้ม พร้อมลูกศรชี้ขวา
- **เอฟเฟกต์**: Hover เพิ่มเงา scale 1.02x

### ปุ่มรายชื่อนักเรียนค้างคืนหนังสือ (สีเทา-ดำ):
- **พื้นหลัง**: Gradient สีเทาเข้ม (#2C2C2C → #3D3D3D → #4A4A4A)
- **ไอคอน**: รูปนาฬิกา (Clock Icon) สีขาว
- **ข้อความ**: "รายชื่อนักเรียนค้างคืนหนังสือ" และคำอธิบาย "ตรวจสอบสถานะการยืม / รายชื่อค้างคืน"
- **ปุ่มกด**: Gradient สีส้ม ข้อความสีขาว พร้อมลูกศรชี้ขวา
- **เอฟเฟกต์**: Hover เพิ่มเงา scale 1.02x

---

## ✅ Checklist การตรวจสอบ

- [x] ปุ่มทั้ง 2 ปุ่มอยู่ในส่วน Hero Section ด้านขวา
- [x] ดีไซน์เลียนแบบจาก Chulabook.com (Gradient, Icons, Shadows)
- [x] ใช้ `<a>` tag แทน `<Link>` เพื่อเปิดหน้าใหม่
- [x] เพิ่ม `target="_blank"` และ `rel="noopener noreferrer"` เพื่อความปลอดภัย
- [x] Responsive ทำงานได้ทั้ง Desktop และ Mobile
- [x] มี Hover effects และ Transitions ที่ลื่นไหล
- [x] ใช้สีตามธีม RWB E-Library (สีส้มพาสเทลและสีเทาเข้ม)
- [x] มีไอคอนที่เหมาะสม (Search และ Clock)
- [x] มีคำอธิบายที่ชัดเจน
- [x] ปุ่มมีขนาดเหมาะสม (h-[95px] บน mobile, h-[135px] บน desktop)

---

## 📝 หมายเหตุ

**สำคัญ:** 
- กรุณาแทนที่ `YOUR_GOOGLE_APPS_SCRIPT_SEARCH_URL_HERE` และ `YOUR_GOOGLE_APPS_SCRIPT_LATE_RETURNS_URL_HERE` ด้วย URL จริงที่ได้จาก Google Apps Script Deploy
- URL ควรมีรูปแบบ: `https://script.google.com/macros/s/{SCRIPT_ID}/exec`
- สามารถเพิ่ม Query Parameters เช่น `?page=search` หรือ `?page=lateReturns` เพื่อแยกหน้า

**ตัวอย่าง URL ที่สมบูรณ์:**
```
https://script.google.com/macros/s/AKfycbzXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX/exec?page=search
https://script.google.com/macros/s/AKfycbzXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX/exec?page=lateReturns
```

---

## 🚀 การทดสอบ

1. บันทึกไฟล์ `/components/HomePage.tsx` และ `/components/Navbar.tsx` หลังจากแทนที่ URL
2. รีเฟรชเว็บไซต์
3. คลิกปุ่ม "เริ่มค้นหา" และ "ดูรายชื่อ"
4. ตรวจสอบว่าเปิดหน้า Google Apps Script ได้ถูกต้อง
5. ทดสอบทั้งบน Desktop และ Mobile

---

## 📞 ติดต่อ

หากมีปัญหาหรือต้องการความช่วยเหลือ กรุณาติดต่อทีมพัฒนา RWB E-Library